<div class="card">
  <h2>{{title}}</h2>
  <p>{{message}}</p>

  <h3>Tu sesión</h3>
  <p><strong>Email:</strong> {{user.email}}</p>
  <p><strong>Rol:</strong> {{user.role}}</p>

  <p style="margin-top:16px">
    <a class="btn" href="/current">Volver al perfil</a>
  </p>
</div>
